'use client';

export function FeatureSection() {
  // This is a placeholder for the scroll height. 
  // Each FeatureSection provides 100vh of space for the R3F scene to persist.
  return (
    <div className="h-screen w-full relative pointer-events-none">
      {/* Empty spacer section - R3F SceneManager is fixed underneath */}
    </div>
  );
}
